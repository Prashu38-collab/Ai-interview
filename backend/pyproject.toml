[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# B008: FastAPI uses Depends() in argument defaults by design. This is the
# documented idiomatic pattern and is not a bug, so we silence the warning.
ignore = ["B008"]
